/*
	If itemIndex is set to 0, the sprite_index will change to s_spellbook.
	If itemIndex is set to 1, the sprite_index will change to s_key.
	Otherwise, the sprite_index will change to s_wasd_keys, so that the error 
	will be obvious because the keys sprite will show which will look out of place.
*/
/*switch(itemIndex){
	case 0: 
		sprite_index = s_spellbook; 
		break;
	case 1:
		sprite_index = s_key;
		break;
	default:
		sprite_index = s_wasd_keys;
		break;
}*/
